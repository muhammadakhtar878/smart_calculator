import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Header } from "@/components/layout/header"
import { Footer } from "@/components/layout/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "OIC Markeït - Islamic Marketplace",
  description: "Your trusted Islamic marketplace offering authentic halal products, modest fashion, Islamic books, and more. Serving the global Muslim community.",
  keywords: ["Islamic marketplace", "halal products", "modest fashion", "Islamic books", "prayer items", "Muslim community"],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen flex flex-col">
          <Header />
          <main className="flex-1">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  )
}
